name: Plugin macOS-only release smoke

on:
  push:

jobs:
  release-macos-only:
    name: Verify released macOS-only graph
    runs-on: macos-14
    steps:
      - name: Verify macOS runtime
        shell: bash
        run: |
          test "${GITHUB_ACTIONS:-}" = true
          test "${RUNNER_OS:-}" = macOS
          test "$(uname -m)" = arm64
